# H027 修改紀錄

## 2026-08-27：C3 初始／掉落轉換機制分離

- 初始盤面的每顆 C2 依「轉換前的初始 C2 總數」各自獨立判定一次是否轉成 C3。
- Cascade 掉落的新 C2 改依 Combo 1、2、3、4、5+ 五個權重欄位獨立判定；Combo 5 以後統一使用 5+。
- 已在場的 C2 不會因後續 Cascade 重抽；已在場的 C3 繼續沿用每次得獎消除升一級的原規則。
- `Simulator.py`、`index.html`、Base／92A／94A Config 與 v1 封存版已同步新機制。
- `H0271.xlsx / Parameter` 新增 `weight_use_super_multiplier_drop`，並更新 XLSX↔Config 轉換工具。
- 確認初始 C2 顆數參數位於 `Parameter!B16:H24`，掉落 Combo 參數位於 `Parameter!B55:G63`；兩區皆為實際 Runtime 輸入。
- 修復 `Source/model_sync.py` 的轉換入口及空白輪帶尾列讀取，Base／v1 Excel→Config 與 Config→Excel→Config 驗證通過。
- `H027192A.xlsx`、`H027194A.xlsx` 維持只管理 Card System 倍率區間；自然 C3 參數不重複存放，以 `H0271.xlsx` 為唯一來源。
- Pa～Pe 初值沿用原本初始球數曲線的前五級；本次只分離機制，尚未重調競品倍率分布。

## 2026-08-26：競品比較報表更新至 v1 目前參數

- `其他/競品比較_H027.md` 改用最新 Card-Off Normal 1,000,000 場、BF 200,000 場，以及 92A／94A Card-On 各 100,000 場抽驗。
- 競品基準改為 2026-08-26 的 `遊戲數據_Gates_of_Olympus_1000.md/.html`：BG／FG RTP `68.536% / 22.212%`、BG／FG Hit `28.807% / 43.564%`、FG 週期 `1/425.3`。
- 更新基本指標、R1～R6 初始／掉落符號、堆疊、獎項占比、BG Combo、倍數球、64 倍率區間與資料限制。
- `其他/update_competitor_comparison.py` 同步改為自動挑選 v1 最新報告，並停止沿用競品最新報告未提供的 FG Combo／FG 有球率舊數值。

## 2026-08-26：改用 Gates of Olympus 1000 還原輪帶

- `config.js`、`config_92A.js`、`config_94A.js` 改用 `還原輪帶_Gates_of_Olympus_1000.xlsx` 的 Reel Set 0～4；BG 選 Set 0～2，FG／BF 免費轉選 Set 3～4。
- `Simulator.py` 與 `index.html` 取消 Symbol Drop Weight、linked drop 與隨機掉落抽取；消除後一律沿當前 Reel 的同一條環狀輪帶補入。
- `Source/H0271.xlsx` 已同步六張輪帶表，Symbol Drop Weight 區清零；XLSX→config→XLSX round-trip 驗證通過。
- 競品缺少 BF 入場專用 Set 6，暫以 Set 0 作為 `BF_Symbol` 並強制 R1～R4 各一顆 C1；免費轉仍使用 Set 3～4。
- Card-Off 100 萬場結果：總 RTP 108.3942%，BG／FG RTP 59.7306%／48.6636%，BG／FG Hit 28.4353%／38.8874%，FG 週期 454.55 spins。
- `其他/競品比較_H027.md` 已依最新 Normal／Buy Feature 各 100 萬場重算。舊 92A／94A 卡片權重建立於上一版自然盤，本次比較不引用其舊抽驗結果，待自然盤確認後重校。

## 2026-08-25：自然模型與卡片倍率線型對標競品

### 修改目的

- 先校正 Card System Off 自然模型，再以卡片收斂 92A／94A RTP 與倍率線型。
- 對標競品的符號分布、堆疊、Combo、倍數球、有／無球 Hit、FG Hit、FG 週期、BG:FG RTP、FG 平均倍數及最大倍數。

### 實際變更

- `config.js`：初始與掉落維持競品 R1～R6 邊際分布，新增跨輪停點及掉落相關性；C2 限制為 2x～8x，10x 以上只由 C3／Super 產生；Super 使用率隨初始球數嚴格遞增。
- `Simulator.py`、`index.html`：同步實作上述停點、掉落與 C1 獨立抽取邏輯。
- `config_92A.js`、`config_94A.js`：競品 64 區間倍率線型只投影至自然命中率至少 0.1% 的區間。
- `Source/H0271.xlsx`、`H027192A.xlsx`、`H027194A.xlsx`：同步目前 Config；自然 XLSX 可完整匯出回 `config.js`。
- `其他/競品比較_H027.md`：改用最新 Card-Off 100 萬場與 92A Card-On Normal／Buy Feature 抽驗資料，逐項列出競品差異。

### 數值結果

- Card-Off 最新 100 萬場：BG RTP 84.9732%、FG RTP 107.2690%、FG Hit 46.0546%、FG 週期 452.49、FG 平均倍數 485.38x、最大得分 59500x。
- 競品容許上限：BG 179.289%、FG 124.340%；Card-Off 兩者皆通過。
- 92A Card-On Normal 抽驗：總 RTP 90.5175%、FG 平均倍數 101.13x。
- 92A Card-On Buy Feature 抽驗：RTP 92.0197%。
- 94A Card-On Normal／Buy Feature 抽驗：RTP 92.4898%／94.7124%；Retry Limit 超限均為 0。

### 驗證

- Python 語法檢查通過。
- `Source/model_sync.py export --source Source/H0271.xlsx --output config.js --check` 通過，結果為 `Config is current`。
- 無頭 Edge 實跑通過：固定盤面比例、Version／Config、Card System 與 Spin 均正常，Console 無嚴重錯誤。

## 2026-08-25：盤面顯示與設定流程依開發規範調整

- Version 只列出該版本可用的 92A／94A Config，移除 Base-Off 選項。
- Card System Off 使用自然機率；On 才套用所選 Profile 的卡片權重。
- 盤面固定 6×5 相對位置，符號格與間距隨可用寬度等比例縮放。
- 修改紀錄在 Debug Mode 的 `#change-log-wrap` 顯示。
# 2026-08-26 — 自然模型 v1：競品停輪權重與 BF 進場盤面

- BG Reel Set 0～2、FG Reel Set 3～4 保留競品原始輪帶長度，依競品 Response 初始盤面反推各輪各 stop 的權重。
- FG stop 權重合併自然 FG 與 Buy Feature FG 樣本，降低自然 FG 樣本較少造成的波動。
- 加入少量共用加權百分位停輪與逐輪相位，在不改各輪符號比例的前提下補回競品跨輪關聯；BG、FG 分開校準。
- BG 的 C1 可見 stop 權重提高 4%，將 FG 週期收斂至競品附近。
- FG 倍數球改為該 Spin 有得分消除時才加入累積倍數池，Index 與 Simulator 同步。
- BG Set 選擇權重更新為 `9698 / 19298 / 2344`。
- Buy Feature 初始盤面改為競品實機方式：R2～R5 各 1 個 C1，其餘 26 格均勻抽取 9 種一般符號。
- 基礎數學版本升為 `1`；92A、94A 對應版本升為 `1.0.0.0`。
- 92A／94A 卡片依 v1 自然報告重新校準；競品目標更新為 BG Hit `28.807%`、FG 週期 `1/425.3`、FG 場均 `94.47x`。
- Card-On oldhand small bet 實測：92A Normal 500,000 轉 RTP `91.3528%`、BF 100,000 次 RTP `91.8604%`；94A Normal 500,000 轉 RTP `94.5282%`、BF 100,000 次 RTP `93.9493%`。
- `H027192A.xlsx`、`H027194A.xlsx` 已同步卡片權重，Base／92A／94A physical model 與 round-trip 驗證通過。
# 2026-08-26 — v1 競品輪帶長度與 C1 邊界修正

- 確認六張表均為競品 Reel Set 0～4 的循環等價序列，實際輪長只有 63／64，並非 200／300。
- 所有有效停點的 `Symbol Weight` 統一改為 1，移除先前 Response-fit 的大型停點權重。
- 各 Reel 只做循環位移，讓 C1 不出現在 RNG 前 5 格或最後 5 格；linked percentile offset 同步補償，避免位移改變既有聯動抽停結果。
- `Source/H0271.xlsx` 超過實際輪長的 Line／Symbol／Symbol ID cache／Weight 改為空白。
- `Versions/1.0/Source/model_sync.py` 同步修正，確保 config→xlsx 後不會再次用 C1 與 Weight 0 填滿尾列。
- 新增 `其他/competitor_reel_validation_v1.json`，保存每輪競品循環比對、實際長度、C1 位置與空白尾列驗證。
- Weight 1 版本 Card-Off 1,000,000 轉：RTP `98.4847%`，BG／FG RTP `57.7380%`／`40.7468%`，BG／FG Hit `29.0266%`／`44.0526%`，FG 週期 `1/452.69`。
- 依新自然報告重新校準 92A／94A 卡片並同步 `H027192A.xlsx`／`H027194A.xlsx`；100,000 次 BF 抽驗 RTP 分別為 `92.4008%`／`93.9058%`，Retry Limit 超限為 0。

# 2026-08-27 — C2／C3 單一倍率池與競品倍率重填

- `weight_use_super_multiplier` 更名並改義為 `weight_C2_to_C3_by_initial_count`：依初始盤面 C2 總數決定每一顆 C2 各自轉成 C3 的機率。
- 保留掉落階段的 Combo 1、2、3、4、5+ 逐顆轉換機率，Config 統一使用 `c2_to_c3`。
- 移除 `weight_super_multiplier`、`parameter.super_multiplier` 與 C2／C3 分離倍率池。
- `multiplier_level_C2_C3` 定義為 C2／C3 共用可出現倍率及 C3 後續升級階層。
- BG／FG／BF 倍率權重重新填入 Gates of Olympus 1000 競品分布，並同步 Base、92A、94A Config、三份 XLSX、Index 與 Simulator。
- 新 Card-Off 模擬：Normal 1,000,000 場總 RTP `112.7074%`，BG／FG RTP `76.9208% / 35.7866%`，BG／FG Hit `29.1391% / 43.5984%`，FG 週期 `1/451.88`；Buy Feature 200,000 場 RTP `173.1858%`。
- `其他/競品比較_H027.md` 已改用上述新報告；目前沒有新機制的 92A／94A 模擬，因此報表省略 Card-On 數值，不使用舊報告或推估值。

# 2026-08-27 — C3 暫停出現與預留倍率表

- 初始盤面與掉落 Combo 的 C2→C3 權重全部改為 0；Index 與 Simulator 均不會自然產生 C3。
- `multiplier` 拆成 `weights_c2`／`weights_c3`；C3 權重複製 C2，但所有 100x 以上倍率權重為 0。
- `H0271.xlsx / Parameter` 新增 C3 預留倍率表，並同步 XLSX↔Config 轉換工具、Base／92A／94A Config 與 v1。
- 停用 C3 後重新模擬：Normal 1,000,000 場總 RTP `110.4403%`、BG／FG RTP `74.2664% / 36.1739%`；Buy Feature 200,000 場 RTP `169.0202%`。

# 2026-08-27 — Parameter 表格重新排版

- XLSX↔Config 轉換工具改讀寫新的 Parameter 座標：C2 倍率 `J2:AI9`、C3 倍率 `J12:AI19`、倍率階層 `B16:C42`、初始 C2→C3 `J22:P30`、掉落 Combo `J32:O40`。
- 根目錄與 v1 的 `H0271.xlsx` 已同步新排版；XLSX→Config 為 current，Config→XLSX 為 `Changed cells: 0`。
