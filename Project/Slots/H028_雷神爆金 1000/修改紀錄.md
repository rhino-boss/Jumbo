# 修改紀錄

## 2026-08-03：新增 Lucky Neko BG Symbol 比例比較文件

- 新增 `BG_symbol_ratio_comparison.md`。
- 比較 `analysis_lucky_neko.xlsx` 的 `SymbolOcc_Init / BG` 原始比例與目前 `config_92A.js` 版本 `2.0.0.5` 的實際輪帶比例。
- R1～R6 逐 Symbol 列出來源比例、目前格數、目前比例與百分點差異；R7 因來源沒有對照資料而獨立列示。
- 驗證目前七輪均為 150 格、位置權重全為 1；R1～R6 平均絕對差異 0.1543 pp，最大絕對差異 0.3771 pp。

## 2026-08-03：改回填 Symbol 並修正 calcChain

- 修正上一版錯誤：config 輪帶不再寫入 `U:AA` 的 Symbol ID／公式結果區。
- `config_to_xlsx.py` 會依各 Symbol 工作表 `A4:J29` 的 ID 對照，將 config 數字 ID 轉成符號名稱後寫入真正的輪帶區 `M:S`。
- `xlsx_to_config.py` 改從 `M:S` 讀取符號名稱並轉回數字 ID，不依賴公式快取值。
- 回填時會恢復 `U:AA` 的原始公式格式，移除失效的 `xl/calcChain.xml` 及其關聯，並設定 Excel 開啟時完整重算。
- 未調整的 `BG_Symbol (2)` 與三組 FG 輪帶已從現有 xlsx 的 `M:S` 重新同步至 config，避免被舊版錯誤 ID 資料覆蓋。
- 已重新直接覆寫 `H028192A.xlsx`；最終雙向差異為 0，`M:S` 150 格 Symbol 完整、`U:AA` 公式完整，OOXML 與 calcChain 清理檢查通過。
- 數學內容與 `config_92A.js` 版本維持 `2.0.0.5`。

## 2026-08-03：config 150 格 BG 輪帶回填 xlsx

- 將目前 `config_92A.js` 直接回填至 `Source/H028192A.xlsx`，未建立另一份 xlsx。
- xlsx 版本同步更新為 `2.0.0.5`。
- `BG_Symbol` 的 R1～R7 輪帶與權重均已寫入 150 格範圍。
- 同步寫入目前的參數組選擇權重，BG／FG／Retrigger 均只會使用第一組工作表。
- 共映射 14,399 格、實際變更 1,234 格，config → xlsx → config round-trip 驗證通過。

## 2026-08-03：BG 七輪長度統一為 150

- `BaseGameSymbol1` 的 R1～R7 全部調整為精確 150 格，對應的 `BaseGameSymbolWeight1` 也全部調整為 150 個權重。
- R1～R6 重新依 Lucky Neko `SymbolOcc_Init`／BG 比例量化為 150 格，並以固定亂數種子 `20260803` 打亂。
- R7／Extra Reel 沒有 Lucky Neko 直接比例，因此依修改前 R7 的有效加權符號比例量化為 150 格。
- 七輪位置權重全部統一為 1，確保輪帶格數比例等於實際抽取比例。
- xlsx ↔ config 共用映射同步由 `U4:AA124`／`AC4:AI124` 延伸為 `U4:AA153`／`AC4:AI153`，讓 `update_xlsx.bat` 可直接回填 150 格輪帶。
- `config_92A.js` 版本由 `2.0.0.4` 升為 `2.0.0.5`。

## 2026-08-03：BG 輪帶套用 Lucky Neko 符號比例

- 依 `其他/參考資料/analysis_lucky_neko.xlsx` 的 `SymbolOcc_Init`／BG／R1～R6 比例，重建 `config_92A.js` 的 `BaseGameSymbol1` 前六輪。
- 每輪維持 121 格，先以最大餘數法換算各 Symbol 數量，再使用固定亂數種子 `20260803` 打亂排列，結果可重現。
- `BaseGameSymbolWeight1` 前六輪統一設為 1，避免舊位置權重使實際抽取比例偏離新輪帶。
- 參考資料沒有 R7／Extra Reel 的直接符號比例，因此第七輪及其權重維持原值。
- 未修改 `BaseGameSymbol2`、FG 輪帶與 Drop 權重。
- `config_92A.js` 版本由 `2.0.0.3` 升為 `2.0.0.4`。

## 2026-08-03：update_xlsx.bat 改為直接覆寫 xlsx

- `update_xlsx.bat` 改用 `config_to_xlsx.py --in-place --overwrite-formulas`。
- 執行後直接更新所選 xlsx，不再建立 `*_from_config_*.xlsx`。
- 批次畫面會明確顯示原地覆寫警告，並提醒先關閉 Excel。
- 完成覆寫後仍會執行 config → xlsx → config round-trip 驗證。
- `config_to_xlsx.py` 直接執行時仍保留另存副本的預設行為。

## 2026-08-03：config 鎖定第一組 BG／FG 參數

- `config_92A.js` 版本由 `2.0.0.2` 升為 `2.0.0.3`。
- `ReelWeight` 由 `[2000, 2000]` 改為 `[4000, 0]`，只會選用 `BG_Symbol`。
- `FreeReelWeight` 由 `[8000, 3000, 4000]` 改為 `[15000, 0, 0]`，FG 初始場次只會選用 `FG_Symbol`。
- `FreeTriggerReel` 由 `[4000, 0, 8000]` 改為 `[12000, 0, 0]`，FG Retrigger 也只會選用 `FG_Symbol`。
- 各選擇器的原始總權重保持不變，未修改任何輪帶或掉落權重內容。

## 2026-08-03：修改紀錄改為 Debug Mode 顯示

- 修改紀錄面板預設隱藏。
- 只有開啟 Debug Mode 時才顯示，關閉 Debug Mode 後立即隱藏。

## 2026-08-03：index 新增修改紀錄區

- 將 `修改紀錄.md` 的內容完整嵌入 index。
- 顯示位置位於 Log 與設定區域之間。
- 內容使用固定高度的可捲動面板，避免影響主要操作區。

## 2026-08-03：重新校正 xlsx / config 雙向位置

### 問題原因

- 目前 `H028192A.xlsx` 已改為每組參數各自一張 Symbol 工作表，但工具仍使用舊版「同張表向右延伸」的位置。
- 輪帶 ID、輪帶權重、MegaWay、Mystery、Post Scatter 與五段 Drop 權重皆存在一欄或一列偏移。
- 參數組選擇權重已由 `Description` 移至 `Parameter`，舊工具仍讀寫原位置。

### 修正內容

- BG 組 1／2 改對應 `BG_Symbol`、`BG_Symbol (2)`。
- FG 組 1／2／3 改對應 `FG_Symbol`、`FG_Symbol (2)`、`FG_Symbol (3)`。
- 共用位置校正為：輪帶 ID `U4:AA124`、權重 `AC4:AI124`、MegaWay `C33:H47`、Mystery `C51:C63`、Post Scatter `B67:C74`。
- Drop 1～5 校正為 `AL:AR`，起始列依序為 4、33、62、91、120，各 26 列。
- `ReelWeight`、`FreeReelWeight`、`FreeTriggerReel` 改讀寫 `Parameter!C5:C6`、`C11:C13`、`C18:C20`。
- `xlsx_to_config.py` 與 `config_to_xlsx.py` 改共用同一份映射常數，避免雙向工具日後各自漂移。
- 依校正後位置重新產生目前使用的 `config_92A.js`。
- 同步重寫 `xlsx_config_usage_mapping.md` 與 `config_xlsx_usage_mapping.md`。

### 驗證

- Python 語法檢查通過。
- xlsx → config 再次讀取比對通過。
- `config_92A.js` 的輪帶為 7×121、MegaWay 為 6×15、Drop 為 7×26，且三組參數選擇權重皆有正確數值。
- config → xlsx → config 完整 round-trip 驗證通過，共映射 13,993 格。

## 2026-08-03：還原 Git 後改用系統預設符號

### 還原範圍

- 先將 `Project/Slots/H028_雷神爆金 1000` 全部已追蹤檔案還原至目前 Git `HEAD`。
- Git 追蹤的 `Source/Image` 圖片素材已恢復，但新版 index 不會使用或載入這些圖片。
- 未追蹤的 `config_92.js` 與 `config_92A_v1.js` 保留，未刪除或修改。

### index 變更

- 新增固定設定 `USE_SYMBOL_IMAGES=false`。
- `symbolArt()` 在系統模式直接回傳空字串，不建立 `<img>` 元素，也不向 `Source/Image` 發出圖片請求。
- 所有有效符號使用 Git 版本原有的系統預設框、背景色與文字代碼。
- 無效、空值或負數符號 ID 顯示空白，不產生 `S-1`，也不套用 Mystery 系統框。
- 未修改 config、Simulator 或遊戲數學。

### 驗證

- H028 主要 inline JavaScript 已通過 Node 語法解析。
- 已確認圖片開關固定為 `false`，且 `symbolArt()` 在任何圖片路徑建立前即返回。

## 2026-08-03：新增 update_xlsx.bat

### 工具內容

- 新增 `Source/update_xlsx.bat`，作為 `config_to_xlsx.py` 的雙擊入口。
- 預設使用：
  - config：專案根目錄的 `config_92A.js`
  - xlsx：`Source/H028192A.xlsx`
- 雙擊執行時會列出並詢問 config 與來源 xlsx；直接按 Enter 使用預設值。
- 命令列亦可依序傳入 config 與 xlsx 路徑。
- 優先使用工作區 `.venv`，找不到時改用 `py -3`。
- 固定另存成 `*_from_config_*.xlsx`，不覆寫原始數學表。
- 同名輸出已存在時使用 `--force` 更新。
- 輸出後由 `config_to_xlsx.py` 自動執行 round-trip 驗證。
- `Source/config_xlsx_usage_mapping.md` 已補上雙擊與命令列使用方式。
