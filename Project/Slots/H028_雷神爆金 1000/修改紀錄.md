# 修改紀錄

## 2026-08-05：Overview 新增 FG 觸發局最大 BG 得分

- `Simulator.py` 的輸出報表 `Base Info`（Overview）新增 `trigger_fg_bg_max_pay`。
- 此欄記錄所有成功觸發 FG 的付費 BG Spin 中，單把 BG 得分的最大值；包含 `bet_multi`，不包含後續 FG Session 得分。
- 僅新增報表統計，不改變 Total／BG／FG RTP、遊戲結果、config、xlsx 來源、權重與版本號。

## 2026-08-04：Overview 新增 FG 觸發局 BG 得分

- `Simulator.py` 比照 H026，在輸出報表 `Base Info`（Overview）新增 `trigger_fg_bg_pay` 與 `trigger_fg_bg_count`。
- `trigger_fg_bg_pay` 只累加成功觸發 FG 之付費 BG Spin 的 BG 得分；該得分仍同時保留在既有 `rtp_bg`／Base Game Pay 中，不改變 Total、BG、FG RTP。
- `trigger_fg_bg_count` 使用既有 FG Trigger 次數，方便核對觸發局平均 BG 得分。
- 未修改 config、xlsx 來源、遊戲權重與版本號。

## 2026-08-04：加亮金框符號底板

- `index.html` 的金框符號比照 C1 使用雙層漸層底板，將上層高光與底層底色調成較明顯的亮黃色／金黃色。
- 保留原本金框邊線、符號文字配色與遊戲判定邏輯。
- 僅調整畫面樣式；未修改 config、xlsx、Simulator、權重與版本號。

## 2026-08-04：關閉 Index Card 系統

- `index.html` 新增前端 Card 功能開關並設定為關閉；一般 Spin 與頁面批次模擬不再依 Card 區間重抽結果。
- Player Profile 與 Debug Mode 的 Card BG Range 選單在 Card 關閉時停用，批次模擬狀態顯示為 `Card System Off`。
- 保留 `config_92A.js` 的 Card 資料與 Python Simulator 設定，未修改 config、xlsx、Simulator、權重與版本號。

## 2026-08-04：移除 H028 MegaWay1 權重表

- 從 `其他/競品參考數值比較.md` 移除「H028 MegaWay1 權重」章節、版型順序及 BG／FG R1～R6 的 15 組權重表。
- 保留前段 SymbolSize／ReelHigh 計算方式與後段 Extra Reel 符號分布內容。
- 僅調整報告顯示；未修改 config、xlsx、Simulator、權重與版本號。

## 2026-08-04：主輪符號比例拆成 R1～R6 欄位

- `其他/競品參考數值比較.md` 的 BG 初始、BG 掉落、FG 初始、FG 掉落四張表，不再用斜線把六輪數值放在同一格。
- 每個 Symbol 改列 Lucky Neko、H028 兩列，並使用 R1、R2、R3、R4、R5、R6 六個獨立欄位逐輪比較。
- 僅調整表格顯示，保留原本各 1,000,000 次模擬數值；未修改 config、xlsx、Simulator、權重與版本號。

## 2026-08-04：主輪符號比例恢復四區塊顯示

- `其他/競品參考數值比較.md` 的主輪符號比例由左右合併表恢復為四個獨立區塊：BG 初始 R1～R6、BG 掉落 R1～R6、FG 初始 R1～R6、FG 掉落 R1～R6。
- 保留前次各 1,000,000 次實際模擬數值與普通／金框 ID 合併口徑，只調整文件顯示方式。
- Extra Reel、消除率、M1、金框比例與其他數值未變更；未修改 config、xlsx、Simulator、權重與版本號。

## 2026-08-04：符號分布改用實際模擬值並合併初始／掉落

- `其他/競品參考數值比較.md` 的主輪符號分布改用目前 H028 模型各 1,000,000 次 BG／FG 初始盤面與掉落抽樣，不再只列參數套用位置。
- 普通／金框 ID 合併回本體 Symbol；初始值取 MY、Post C1、Extra Reel 併入後的實際 block head，掉落值取實際新補入 Symbol。
- Base Game 與 Free Game 各自用一張表，將初始 Lucky／H028 與掉落 Lucky／H028 並排；每個 Symbol 依 R1→R6 顯示。
- Extra Reel 的初始與消除掉落合併成左右並列表，BG／FG 分開。
- M1 出現率表移除「結論」欄及表後結論文字。未修改 config、xlsx、Simulator、輪帶、權重與版本號。

## 2026-08-04：競品比較改為數值表格並加入金框模擬比例

- `其他/競品參考數值比較.md` 的 RTP、BG Hit Rate、FG Trigger Rate／週期與平均 Free Spins 合併為單一核心指標比較表，補充說明集中放在表後。
- Base Game／Free Game 符號套用說明改為表格；Extra Reel 初始與消除掉落分別拆成 BG／FG，新增 Lucky Neko、H028 與百分點差異。
- 消除率拆成 BG、FG 兩張表，維持 `0、1、2、3、4、5+` 口徑。
- 「金銀框數量」改為「金銀框比例」；H028 依目前盤面生成與掉落邏輯，各抽樣 1,000,000 次 BG／FG，對比 Lucky Neko 銀框比例。
- 模擬顯示 Drop 金框比例已接近競品；初始金框比例主要在 BG R2～R5 偏低 4.5738～7.6386 pp。本次未修改 config、xlsx、Simulator、輪帶、權重與版本號。

## 2026-08-04：重整競品參考數值比較章節

- 將 `其他/競品參考數值比較.md` 依指定架構重整為：目錄、Overvire、賠率、符號分布、消除率、M1 出現率、金銀框數量、符號大小分布 vs 輪高、Extra Reel 符號分布。
- 符號分布再拆為 BG／FG 初始、BG／FG 消除掉落、Extra Reel 初始與 Extra Reel 消除掉落；最後一章專門整理 `ExtraReelSame` 的排列結果，避免與邊際符號比例重複。
- Overview 新增 Lucky Neko 與 H028 2.0.0.11 比較表；H028 使用目前 BG 第一顆 M1 規則、Card 關閉、1,000,000 局 Normal Bet 抽樣。
- 補入賠率表、RTP Total／BG／FG、BG Hit Rate、FG 觸發週期、0～5+ 消除率、M1 出現率、銀框／金框期望數量、SymbolSize、ReelHigh 與 MegaWay 權重。
- 本次只修改 Markdown 報告及 Debug 修改紀錄，未修改 config、xlsx、Simulator、輪帶、權重與版本號。

## 2026-08-04：區分 BG 第一顆 M1 與 FG 累加規則

- 依規則重新校正 index 與 Simulator：BG 第一顆 M1 用符號大小建立倍數，1×1 使 x1→x2、2×1 使 x1→x3、3×1 使 x1→x4、4×1 使 x1→x5。
- BG 後續出現的 M1 完整累加標示值；若初始盤面沒有 M1，第一次由主輪或 Extra Reel 掉落補入的 M1 同樣套用「第一顆建立倍數」規則。
- FG 不套用 BG 第一顆規則，所有初始與掉落 M1 都直接對目前累積倍數加上標示值，例如 x2 固定加 2。
- M1 是否出現的機率不受本次倍率算法修正影響；前次 100 萬次抽樣比較數值維持不變。未修改 config、xlsx、輪帶、權重與版本號。

## 2026-08-04：M1 倍率校正、移除 Ways 狀態與新增連消分布顯示

> 此筆曾將 BG 第一顆 M1 也改成完整加值；已由上一筆「區分 BG 第一顆 M1 與 FG 累加規則」修正。

- 移除主畫面特色狀態列的 `Ways N` 顯示；Ways 仍保留在 Debug 的逐筆得分明細中。
- 修正 index 與 Simulator 的 M1 累加規則：M1 標示 `x2` 時固定對目前倍數加 `+2`，不再讓 BG 第一顆或第一次掉落的 M1 額外扣 1；BG 從基礎 x1 變成 x3，FG 則從當前倍數再加 2。
- 檢查 FG：初始與掉落 M1 原本已完整加值；檢查 BG：初始、主輪掉落、Extra Reel 掉落三處均已統一修正。
- index 的 Debug 批次模擬新增 BG／FG 連續消除分布表；Simulator 報表新增 `Cascade Dist` 分頁，兩者都固定顯示 `0、1、2、3、4、5+`。
- M1 同口徑各抽樣 1,000,000 次：H028 BG 23.9095%、FG 42.8957%；Lucky Neko BG 16.7661%、FG 35.2864%，目前 H028 分別高 7.1434 與 7.6093 個百分點，尚未對齊。
- 100,000 局 Normal Bet 與 2,000 局 Feature Buy 驗證通過；BG／FG 連消分布筆數均與對應 Spin 數守恆，觀察到的 BG M1 最低結果為 x3。未修改 config、xlsx、輪帶、權重與版本號。

## 2026-08-04：新增 H028 與 Lucky Neko 連續消除分布比較

- 使用目前已修正「大型符號在 Ways 中只算一個符號」的 H028 模型，在 Card 關閉下以固定種子 `20260804` 模擬 1,000,000 局 Normal Bet。
- 以 `analysis_lucky_neko_final.xlsx` 的 `ComboRate`／`Rounds` 為競品基準，統一成 0、1、2、3、4、5+ 次消除口徑；Lucky Neko FG 漏列的 Combo 1 依總樣本數補回 965 筆未中獎 Free Spin。
- 結論為分布不相同：H028 BG 的 2 次以上消除為 20.94%，Lucky Neko 為 8.34%；FG 分別為 23.09% 與 15.01%。
- 新增 `其他/分析報告/連續消除分布比較/`，包含可重跑 Python、SQL 明細、驗證過的 artifact 與自包含 HTML 報告。
- 報告通過資料與 artifact 結構驗證；本機 Chrome 的 chart extraction 環境不相容，因此 HTML 保留 structural-only 驗證。未修改 config、xlsx、輪帶、權重與版本號。

## 2026-08-04：大型符號在 Ways 中只計一個符號

- 修正 `Simulator.py` 與 `index.html`：大型符號無論覆蓋幾格，在單輪 Ways 數量中都只計 1 個符號，不再以符號高度重複累計。
- 同步修正 `game_help_draft.md` 與 index 內嵌 Help 的中英文規則說明。
- 以截圖局面重算：M4 為 2 Ways／0.80、M5 為 1 Way／0.30、K 為 4 Ways／1.20、J 為 1 Way／0.05，該次 Cascade 合計由錯誤的 29.80 修正為 2.35。
- Numba nopython 主模擬迴圈重新編譯成功，局數與得分區間筆數守恆檢查通過。
- 本次未修改輪帶、權重、`config_92A.js`、xlsx 與版本號。

## 2026-08-04：完整主模擬迴圈改用 Numba

- 將 `simulator_chunk`、參數組抽選、Card 條件判斷、Free Game Session 與統計累加納入 Numba nopython／nogil 編譯路徑。
- `run_simulation` 在正式計時前先預熱完整主迴圈，避免首次 JIT 編譯時間混入模擬耗時。
- Normal Bet、Card 關閉、100,000 局驗證：單執行緒約 214,412 局／秒，18 執行緒約 1,363,983 局／秒；相同環境 H026 約 1,940,278 局／秒。
- 舊 Python 外層與新 Numba 外層各跑 100,000 局，RTP、FG Trigger 與命中率落在相近統計範圍；局數、門檻筆數、Scatter 筆數及場景筆數守恆檢查通過。
- 本次只調整 `Simulator.py` 執行效率，未修改數學參數、`config_92A.js`、xlsx 與版本號。

## 2026-08-04：R7 重複群組分隔且最多四連

- BG／FG R7 每個 Symbol 的總格數維持不變。
- 同一 Symbol 最多連續 4 格；任兩段長度至少 2 的重複群組之間，強制插入至少 1 格單獨 Symbol。
- 驗證結果：BG 14 段、FG 18 段獨立四同符；兩者最大連續長度均為 4，相鄰重複群組均為 0。
- 四同符維持 BG 7.0%、FG 9.0%；至少兩同符調整為 BG 35.0%、FG 46.5%。BG 的 35% 是 14 段獨立四同符必然形成的理論下限。
- config／xlsx 版本由 `2.0.0.10` 升為 `2.0.0.11`，已直接覆寫 `Source/H028192A.xlsx`。

## 2026-08-04：增加 R7 四個相同符號相連

- R7 各 Symbol 的 200 格總數保持不變，再次調整 BG／FG 排列順序。
- 四同符採 Lucky Neko 目標上方最近的 0.5 pp 可達值：BG 由 6.5% 提高至 7.0%，FG 由 8.5% 提高至 9.0%。
- 至少兩同符仍維持 BG 29.5%（目標 29.237693%）、FG 45.0%（目標 44.934211%）。
- config／xlsx 版本由 `2.0.0.9` 升為 `2.0.0.10`，已直接覆寫 `Source/H028192A.xlsx`。

## 2026-08-04：R7 排列校正 ExtraReelSame

- 保留 `Extra Reel_Init` 決定的 BG／FG R7 各 Symbol 格數，只重新排列 200 格輪帶，不改變邊際符號比例。
- 依 `ExtraReelSame` 最佳化循環四格視窗，目標包含每個 Symbol 的四格全同符率與至少兩格同符率。
- BG 四同符由 0% 調為 6.5%（目標 6.712378%），至少兩同符由 56.5% 調為 29.5%（目標 29.237693%）。
- FG 四同符由 0.5% 調為 8.5%（目標 8.684211%），至少兩同符由 50% 調為 45%（目標 44.934211%）。
- config／xlsx 版本由 `2.0.0.8` 升為 `2.0.0.9`，已直接覆寫 `Source/H028192A.xlsx`。

## 2026-08-04：合併競品比例報告

- 將 `Lucky_Neko_ratio_sync_report.md` 與 `BG_symbol_ratio_comparison.md` 整理為單一文件 `其他/競品參考數值比較.md`。
- 目前 `2.0.0.8 / 200 格 / analysis_lucky_neko_final.xlsx` 的正式結果與舊版 `2.0.0.5 / 150 格` 歷史比較分區呈現，避免混用基準。
- 保留同步範圍、金框與 Mega Way 換算方式、權重、驗證結果，以及歷史最大量化差異與舊版 R7 分布。

## 2026-08-04：改用 analysis_lucky_neko_final 完整同步比例

- 唯一參考資料改為 `其他/參考資料/analysis_lucky_neko_final.xlsx`。
- `SymbolOcc_Init`／`SymbolOcc_Drop` 分別套用初始與掉落分布；每輪 200 格，來源比例非 0 的本體 Symbol 至少保留 1 顆非金框符號。
- `Extra Reel_Init` 與 `Extra Reel_Drop` 分別套用至初始 R7 與消除補入 R7，不再共用 `ExtraReelSame`。
- `Silver_Init`／`Silver_Drop` 轉為 H028 金框分布，Drop1～Drop5 使用相同權重。
- 所有 Symbol 皆套用 `SymbolSize` 的 symbol 12 大小比例，再搭配 `ReelHigh_Init` 換算 `BaseGameMegaWay1`／`FreeGameMegaWay1` 的 15 組權重。
- config／xlsx 版本為 `2.0.0.8`；已直接覆寫 `Source/H028192A.xlsx`，雙向差異為 0，`calcChain.xml` 不存在。

## 2026-08-04：啟用中的 BG／FG 每輪擴充為 200 格

- `BaseGameSymbol1`、`BaseGameSymbolWeight1`、`FreeGameSymbol1`、`FreeGameSymbolWeight1` 的 R1～R7 全部改為精確 200 格。
- 依 Lucky Neko 既有的 `SymbolOcc_Init`、`Silver_Init` 與 `ExtraReelSame` 比例重新量化輪帶；Drop 權重保持上一版同步結果。
- `BG_Symbol`／`FG_Symbol` 的 Symbol、Symbol ID 公式與權重範圍延伸為第 4～203 列。
- `config_92A.js` 與 `Source/H028192A.xlsx` 版本由 `2.0.0.6` 升為 `2.0.0.7`。
- config → xlsx 與 xlsx → config 雙向差異皆為 0；14 條啟用輪帶、14 條權重與兩張表的 1,400 格公式均通過檢查。
- `calcChain.xml` 維持移除狀態，未啟用的 BG／FG 參數組仍維持原本 121 格。

## 2026-08-03：同步 Lucky Neko 初始、掉落、銀框與 R7 比例

- 版本由 `2.0.0.5` 升為 `2.0.0.6`，並直接同步覆寫 `Source/H028192A.xlsx`。
- `SymbolOcc_Init` 的 BG／FG R1～R6 比例套用至啟用中的 `BaseGameSymbol1`／`FreeGameSymbol1`。
- `SymbolOcc_Drop` 的 BG／FG R1～R6 比例套用至 Drop1～Drop5。
- `Silver_Init`／`Silver_Drop` 轉換為 ID 13～23 的初始輪帶格數與 Drop 權重；本體 Symbol 折回後仍維持來源比例。
- `ExtraReelSame` 的 BG／FG rate 正規化後套用至初始 R7 與 Drop R7，非零來源 Symbol 至少保留一格。
- 新增可重複執行工具 `Source/apply_lucky_neko_ratios.py` 與驗證文件 `Lucky_Neko_ratio_sync_report.md`。
- config → xlsx 與 xlsx → config 雙向差異皆為 0，工作簿不含 `calcChain.xml`。

## 2026-08-03：新增 Lucky Neko BG Symbol 比例比較文件

- 新增 `BG_symbol_ratio_comparison.md`。
- 比較 `analysis_lucky_neko.xlsx` 的 `SymbolOcc_Init / BG` 原始比例與目前 `config_92A.js` 版本 `2.0.0.5` 的實際輪帶比例。
- R1～R6 逐 Symbol 列出來源比例、目前格數、目前比例與百分點差異；R7 因來源沒有對照資料而獨立列示。
- 驗證目前七輪均為 150 格、位置權重全為 1；R1～R6 平均絕對差異 0.1543 pp，最大絕對差異 0.3771 pp。

## 2026-08-03：改回填 Symbol 並修正 calcChain

- 修正上一版錯誤：config 輪帶不再寫入 `U:AA` 的 Symbol ID／公式結果區。
- `config_to_xlsx.py` 會依各 Symbol 工作表 `A4:J29` 的 ID 對照，將 config 數字 ID 轉成符號名稱後寫入真正的輪帶區 `M:S`。
- `xlsx_to_config.py` 改從 `M:S` 讀取符號名稱並轉回數字 ID，不依賴公式快取值。
- 回填時會恢復 `U:AA` 的原始公式格式，移除失效的 `xl/calcChain.xml` 及其關聯，並設定 Excel 開啟時完整重算。
- 未調整的 `BG_Symbol (2)` 與三組 FG 輪帶已從現有 xlsx 的 `M:S` 重新同步至 config，避免被舊版錯誤 ID 資料覆蓋。
- 已重新直接覆寫 `H028192A.xlsx`；最終雙向差異為 0，`M:S` 150 格 Symbol 完整、`U:AA` 公式完整，OOXML 與 calcChain 清理檢查通過。
- 數學內容與 `config_92A.js` 版本維持 `2.0.0.5`。

## 2026-08-03：config 150 格 BG 輪帶回填 xlsx

- 將目前 `config_92A.js` 直接回填至 `Source/H028192A.xlsx`，未建立另一份 xlsx。
- xlsx 版本同步更新為 `2.0.0.5`。
- `BG_Symbol` 的 R1～R7 輪帶與權重均已寫入 150 格範圍。
- 同步寫入目前的參數組選擇權重，BG／FG／Retrigger 均只會使用第一組工作表。
- 共映射 14,399 格、實際變更 1,234 格，config → xlsx → config round-trip 驗證通過。

## 2026-08-03：BG 七輪長度統一為 150

- `BaseGameSymbol1` 的 R1～R7 全部調整為精確 150 格，對應的 `BaseGameSymbolWeight1` 也全部調整為 150 個權重。
- R1～R6 重新依 Lucky Neko `SymbolOcc_Init`／BG 比例量化為 150 格，並以固定亂數種子 `20260803` 打亂。
- R7／Extra Reel 沒有 Lucky Neko 直接比例，因此依修改前 R7 的有效加權符號比例量化為 150 格。
- 七輪位置權重全部統一為 1，確保輪帶格數比例等於實際抽取比例。
- xlsx ↔ config 共用映射同步由 `U4:AA124`／`AC4:AI124` 延伸為 `U4:AA153`／`AC4:AI153`，讓 `update_xlsx.bat` 可直接回填 150 格輪帶。
- `config_92A.js` 版本由 `2.0.0.4` 升為 `2.0.0.5`。

## 2026-08-03：BG 輪帶套用 Lucky Neko 符號比例

- 依 `其他/參考資料/analysis_lucky_neko.xlsx` 的 `SymbolOcc_Init`／BG／R1～R6 比例，重建 `config_92A.js` 的 `BaseGameSymbol1` 前六輪。
- 每輪維持 121 格，先以最大餘數法換算各 Symbol 數量，再使用固定亂數種子 `20260803` 打亂排列，結果可重現。
- `BaseGameSymbolWeight1` 前六輪統一設為 1，避免舊位置權重使實際抽取比例偏離新輪帶。
- 參考資料沒有 R7／Extra Reel 的直接符號比例，因此第七輪及其權重維持原值。
- 未修改 `BaseGameSymbol2`、FG 輪帶與 Drop 權重。
- `config_92A.js` 版本由 `2.0.0.3` 升為 `2.0.0.4`。

## 2026-08-03：update_xlsx.bat 改為直接覆寫 xlsx

- `update_xlsx.bat` 改用 `config_to_xlsx.py --in-place --overwrite-formulas`。
- 執行後直接更新所選 xlsx，不再建立 `*_from_config_*.xlsx`。
- 批次畫面會明確顯示原地覆寫警告，並提醒先關閉 Excel。
- 完成覆寫後仍會執行 config → xlsx → config round-trip 驗證。
- `config_to_xlsx.py` 直接執行時仍保留另存副本的預設行為。

## 2026-08-03：config 鎖定第一組 BG／FG 參數

- `config_92A.js` 版本由 `2.0.0.2` 升為 `2.0.0.3`。
- `ReelWeight` 由 `[2000, 2000]` 改為 `[4000, 0]`，只會選用 `BG_Symbol`。
- `FreeReelWeight` 由 `[8000, 3000, 4000]` 改為 `[15000, 0, 0]`，FG 初始場次只會選用 `FG_Symbol`。
- `FreeTriggerReel` 由 `[4000, 0, 8000]` 改為 `[12000, 0, 0]`，FG Retrigger 也只會選用 `FG_Symbol`。
- 各選擇器的原始總權重保持不變，未修改任何輪帶或掉落權重內容。

## 2026-08-03：修改紀錄改為 Debug Mode 顯示

- 修改紀錄面板預設隱藏。
- 只有開啟 Debug Mode 時才顯示，關閉 Debug Mode 後立即隱藏。

## 2026-08-03：index 新增修改紀錄區

- 將 `修改紀錄.md` 的內容完整嵌入 index。
- 顯示位置位於 Log 與設定區域之間。
- 內容使用固定高度的可捲動面板，避免影響主要操作區。

## 2026-08-03：重新校正 xlsx / config 雙向位置

### 問題原因

- 目前 `H028192A.xlsx` 已改為每組參數各自一張 Symbol 工作表，但工具仍使用舊版「同張表向右延伸」的位置。
- 輪帶 ID、輪帶權重、MegaWay、Mystery、Post Scatter 與五段 Drop 權重皆存在一欄或一列偏移。
- 參數組選擇權重已由 `Description` 移至 `Parameter`，舊工具仍讀寫原位置。

### 修正內容

- BG 組 1／2 改對應 `BG_Symbol`、`BG_Symbol (2)`。
- FG 組 1／2／3 改對應 `FG_Symbol`、`FG_Symbol (2)`、`FG_Symbol (3)`。
- 共用位置校正為：輪帶 ID `U4:AA124`、權重 `AC4:AI124`、MegaWay `C33:H47`、Mystery `C51:C63`、Post Scatter `B67:C74`。
- Drop 1～5 校正為 `AL:AR`，起始列依序為 4、33、62、91、120，各 26 列。
- `ReelWeight`、`FreeReelWeight`、`FreeTriggerReel` 改讀寫 `Parameter!C5:C6`、`C11:C13`、`C18:C20`。
- `xlsx_to_config.py` 與 `config_to_xlsx.py` 改共用同一份映射常數，避免雙向工具日後各自漂移。
- 依校正後位置重新產生目前使用的 `config_92A.js`。
- 同步重寫 `xlsx_config_usage_mapping.md` 與 `config_xlsx_usage_mapping.md`。

### 驗證

- Python 語法檢查通過。
- xlsx → config 再次讀取比對通過。
- `config_92A.js` 的輪帶為 7×121、MegaWay 為 6×15、Drop 為 7×26，且三組參數選擇權重皆有正確數值。
- config → xlsx → config 完整 round-trip 驗證通過，共映射 13,993 格。

## 2026-08-03：還原 Git 後改用系統預設符號

### 還原範圍

- 先將 `Project/Slots/H028_雷神爆金 1000` 全部已追蹤檔案還原至目前 Git `HEAD`。
- Git 追蹤的 `Source/Image` 圖片素材已恢復，但新版 index 不會使用或載入這些圖片。
- 未追蹤的 `config_92.js` 與 `config_92A_v1.js` 保留，未刪除或修改。

### index 變更

- 新增固定設定 `USE_SYMBOL_IMAGES=false`。
- `symbolArt()` 在系統模式直接回傳空字串，不建立 `<img>` 元素，也不向 `Source/Image` 發出圖片請求。
- 所有有效符號使用 Git 版本原有的系統預設框、背景色與文字代碼。
- 無效、空值或負數符號 ID 顯示空白，不產生 `S-1`，也不套用 Mystery 系統框。
- 未修改 config、Simulator 或遊戲數學。

### 驗證

- H028 主要 inline JavaScript 已通過 Node 語法解析。
- 已確認圖片開關固定為 `false`，且 `symbolArt()` 在任何圖片路徑建立前即返回。

## 2026-08-03：新增 update_xlsx.bat

### 工具內容

- 新增 `Source/update_xlsx.bat`，作為 `config_to_xlsx.py` 的雙擊入口。
- 預設使用：
  - config：專案根目錄的 `config_92A.js`
  - xlsx：`Source/H028192A.xlsx`
- 雙擊執行時會列出並詢問 config 與來源 xlsx；直接按 Enter 使用預設值。
- 命令列亦可依序傳入 config 與 xlsx 路徑。
- 優先使用工作區 `.venv`，找不到時改用 `py -3`。
- 固定另存成 `*_from_config_*.xlsx`，不覆寫原始數學表。
- 同名輸出已存在時使用 `--force` 更新。
- 輸出後由 `config_to_xlsx.py` 自動執行 round-trip 驗證。
- `Source/config_xlsx_usage_mapping.md` 已補上雙擊與命令列使用方式。
