# 遊戲發想

發想中的遊戲（尚無 Game ID）放這裡，一個遊戲一個資料夾，資料夾名就是遊戲名：

```
Slots/其他/遊戲發想/<遊戲名>/
├── game_rule.md   ← 有這個，Omniplay 卡片會顯示玩法與「遊戲資訊」
└── index.html     ← 有這個，卡片會出現「開始遊玩」
```

Omniplay 的「遊戲發想區」會自動掃這裡的資料夾；確定立項拿到代號後，
把資料夾改名成 `H0xx_遊戲名` 並搬回 `Slots/` 頂層，就會轉入 Demogame 區。
