# 送驗文件規範

本文件屬於 `Slots/專案需知/` 專案需知文件集，總覽、共通原則與完成條件見 [_README.md](_README.md)。

內容涵蓋送驗（BMM 等實驗室）交付物的命名、格式來源、內容規則、禁止內容與交付前檢查：**Game Description（DOCX）**、**Math Document Description**、**數學模型送驗版**（送驗包 `PARsheet/` 資料夾的內容物）。

- Game Rule、Help、`game_description.md`、Game List 的內容規範見[數學文件規範.md](數學文件規範.md)；PARsheet 的內容本體規範見[數學模型規範.md](數學模型規範.md)。本文件只規範送驗交付面的要求。
- 製作與檢查的操作流程由對應 skill 執行（製作 Game Description：`game-description-generator`；檢查送驗文件：`math-doc-checker`）；規範內容以本文件為準，skill 與本文件不一致時，先修正其一使兩者一致。

## 1. 通用規則

### 1.1 存放位置與資料夾格式

Help 與送驗文件都放在遊戲專案的 `文件/` 資料夾。外層放**完整版**文件（內容不做任何刪減），送驗專屬的刪減版整理成**以 Game ID 命名的送驗文件包**：

```text
文件/
├─ {GameID}_Help.xlsx
├─ {GameID}_Game Description.docx             ← 完整版：內容同 Help，不移除任何項目
├─ {GameID}_Math Document Description.md      ← 外層維護（中文）
├─ {GameID}_Math Document Description_EN.md   ← 外層維護（英文）
├─ PARsheet/                                  ← 專案的正式 PARsheet（數學模型規範定義的精簡規格文件）
│  └─ H0xx19x.xlsx                             ← 各 RTP 版本
└─ {GameID}/                                  ← 送驗文件包
   ├─ {GameID}_Game Description.docx           ← 送驗版：移除 1.3 禁止項目
   ├─ PARsheet/                                ← 注意：資料夾名雖為 PARsheet，
   │  └─ H0xx19x.xlsx                          ←   放的是「數學模型送驗版」——由 Source
   │                                                數學模型移除 1.3 禁止項目後產出
   └─ Simulator/
      ├─ Simulator.py
      └─ config_9x.js                          ← 各 RTP 版本的 Config
```

- **Game Description 有兩份**：外層的完整版內容與 Help 完全相同、不刪任何項目；送驗包內的送驗版另外移除 1.3 禁止項目。兩份除刪減處外必須一致。
- **Math Document Description 放外層**（md＋`_EN.md`），送驗時連同送驗包內容一起複製進送驗批次資料夾（`送驗\送驗資料\{批次}\{GameID}\`）。
- 送驗包內的 `PARsheet/` 是送驗批次的既定資料夾名稱，內容物是**數學模型送驗版**，不是[數學模型規範.md](數學模型規範.md)定義的 PARsheet 精簡文件；兩者不可混淆。
- 數學模型送驗版由 `Source/*.xlsx` 產出：移除 1.3 禁止出現的項目（Extra Bet／Buy Feature／Super Feature 與 >$2 門檻相關的工作表、欄位與敘述）後另存，數學內容其餘部分與正式數學模型一致。

範例：`H026_彩罐熱舞 1000/文件/`，送驗批次對應 `260805_BMM送驗_101014_101025/101014/`。

### 1.2 必要檔案與命名

| 文件 | 檔名 | 位置 |
|---|---|---|
| Game Description（完整版） | `{GameID}_Game Description.docx`；內容同 Help，不移除任何項目 | `文件/` |
| Game Description（送驗版） | `{GameID}_Game Description.docx`（無其他前後綴）；移除 1.3 禁止項目 | `文件/{GameID}/` |
| Math Document Description | `{GameID}_Math Document Description.md` 與 `{GameID}_Math Document Description_EN.md`；送驗時複製進送驗批次 | `文件/` |
| PARsheet（精簡規格文件） | 各 RTP 版本一個 `.xlsx`，檔名包含 ParSheet ID | `文件/PARsheet/` |
| 數學模型送驗版 | 由 `Source/*.xlsx` 移除 1.3 禁止項目後產出，每個送驗 RTP 版本一個，檔名包含 ParSheet ID（例：`H026192.xlsx`） | `文件/{GameID}/PARsheet/`（送驗批次既定資料夾名） |
| Simulator | `Simulator.py` 與各 RTP 版本的 `config_*.js` | `文件/{GameID}/Simulator/` |

- Game ID 以 `iGaming 遊戲代號一覽.xlsx` 為準，查得英文名稱、中文名稱、ParSheet ID 與 Extra Bet／Buy Feature／Super Feature 支援情況。
- ParSheet ID 前 4 碼（例如 `H026`）對應 `Slots/` 下的 `H0xx_遊戲名稱` 專案資料夾。
- 文件內出現的遊戲英文名稱必須與代號一覽一致；檔名前綴必須與 Game ID 一致。
- 代號一覽查不到該 Game ID 時，先確認來源，不得逕行送驗。

### 1.3 禁止出現的內容

送驗版三份文件內，下列內容一律不得出現（不分大小寫搜尋，含表格儲存格）：

| 檢查項目 | 關鍵字 |
|---|---|
| Extra Bet 敘述 | `extra bet` |
| Buy Feature 敘述 | `buy feature` |
| Super Feature 敘述 | `super feature` |
| >$2 拉彩金門檻 | `>$2`、`> $2`、`bet $2`、`$2 or more`、`more than $2` |

找到即為錯誤，必須移除後才可送驗。

### 1.4 用詞一致性

用詞不一致不會讓數值出錯，但送驗時容易被挑，屬警告等級（明顯錯字升為錯誤）：

- **單一文件內部**：同一概念不得混用大小寫／連字號／空格變體（`GOLD-FRAME` vs `GOLD-FRAMED`、`Free Game` vs `FreeGame`）、單複數（`Free Spin` vs `Free Spins` 同語境混用）或同義換名（同一機制一下 `Free Game`、一下 `Free Spin`；一下 `Wild`、一下 `Joker`）。
- **跨三份文件**：功能名稱、符號名稱、RTP 分類名（Base Game／Free Game／Bonus／Link）在 Game Description、Math Document Description、PARsheet 之間必須同名。
- **中英對照**：同一中文概念只對應一組英文譯名（「免費遊戲」不得一處對 `Free Game`、另一處對 `Free Spin`）。

## 2. Game Description（DOCX）

Game Description 產出**兩份**：

- **完整版**（`文件/`）：內容與 Help 完全相同，不移除任何項目。
- **送驗版**（`文件/{GameID}/`）：在完整版基礎上移除 1.3 禁止項目（Extra Bet／Buy Feature／Super Feature 與 >$2 門檻相關章節與敘述）。

以下 2.1～2.3 的規則兩份皆適用；1.3 禁止內容檢查只適用送驗版，兩份除刪減處外必須完全一致。

### 2.1 格式來源：複製已核准模板

格式（字型、字級、邊界、標題與表格樣式）一律**複製既有已核准的 Game Description 文件**，不重新自建格式——公司對格式的核准是一次性的，自行重建有偏離已核准樣式的風險。

已知兩種版型，依模板實際版面選擇：

| 版型 | 特徵 | 用途 |
|---|---|---|
| A：純段落 | 每行規則為一般未條列段落 | 一般範例（`H000_範例`） |
| B：■ 條列＋框線賠率表 | 每行規則前有 ■（Word 編號清單），賠率表在框線表格內 | BMM 送驗版常用 |

- 選模板時避免內嵌整頁背景圖或遊戲 Logo 的文件，否則舊遊戲的美術會帶進新文件。
- 版型判斷以模板實際內容為準，不假設所有模板結構相同。

### 2.2 內容來源：Help 英文欄逐字填寫

- 內容以該遊戲的 `{GameID}_Help.xlsx` 或 `game_help_draft.md` 的**英文欄逐字填寫**，不得改寫、摘要或自創文字——Help 是已核准的規則措辭唯一來源，改寫會製造兩份互相矛盾的文件。
- **一律使用英文欄**：簡中欄曾出現英文欄沒有的錯字（例如符號誤標 `[M4]`，英文欄正確為 `[M5]`）。
- 去除行首手動 bullet 字元（`▪`／`•`）；版型 B 的 ■ 來自模板的清單樣式，不是文字本身。
- 佔位符（`[WW]`、`[C1]`、`{12}`、`X2` 等）原樣保留；`{n}` 花括號要嘛全篇保留、要嘛全篇展開為實際數字，不得混用。
- 保持 Help 的章節順序，不重排、不遺漏；純圖片佔位列（`[走線圖 ...]`、`[跳階示意圖]`）跳過。
- Help 的章節外層標籤對應輸出的 Heading 2；注意慣例差異（Help 寫 `GAME RULE`，文件慣例為 `GAME RULES`），依既有文件慣例為準，不盲抄。

### 2.3 結構

- Title 樣式段落「GAME DESCRIPTION」＋置中一般樣式段落放遊戲英文名。
- 每個 feature／規則章節一個 Heading 2（PAYTABLE、CASCADING FEATURE、MULTIPLIER FEATURE、FREE GAME FEATURE、OP JACKPOT、GAME RULES 等，依遊戲實際內容）。
- 副標籤（`WILD SYMBOL`、`SCATTER SYMBOL` 等）使用一般段落樣式，不自創粗體或 Heading 3 樣式。
- 賠率表為 3 欄表格，每個 payout tier 一列。

### 2.4 交付前驗證

- 重新解析輸出文件（逐段樣式＋文字、逐表格列），確認：Help 的每個章節都已進入文件、賠率表列數與 Help 的 payout tier 數一致、無殘留 placeholder（`__TABLE__`、`None`、空 run）。
- 送驗版執行 1.3 禁止內容檢查；兩份皆執行 1.4 用詞一致性檢查，並確認兩份除刪減處外完全一致。
- 完成後完整版存入 `文件/`，送驗版存入 `文件/{GameID}/`。

## 3. Math Document Description

### 3.1 定位與範本

針對數學文件（`Source/*.xlsx`）的說明文件：說明各工作表用途與程式讀取範圍、Parameter 參數、遊戲邏輯與卡片系統。

- 交付為 Markdown 雙語兩檔：`{GameID}_Math Document Description.md`（中文）與 `{GameID}_Math Document Description_EN.md`（英文），置於 `文件/` 外層；送驗時複製進送驗批次資料夾。範本參考 `H026_彩罐熱舞 1000/文件/101014_Math Document Description.md`。
- 過往他人專案曾以 DOCX 交付；新專案一律採 md＋`_EN.md` 格式。

### 3.2 內容要求

- 開頭為基本資訊表：Game ID、Math files、Version、對應程式（Simulator／Config）、盤面、線數／得分方式、Coin In、押注模式。
- 四大章：①工作表說明（每張工作表標明程式是否讀取）②Parameter 工作表的遊戲參數說明 ③遊戲邏輯（spin 流程、判獎、Cascade、FG）④卡片系統說明。
- 公式／變數名稱：必須定義 `Ptheory`、`Pscatter`、`Phit`；`Phit` 公式必須使用 `Pscatter`（不得寫成 `Prespin`、`Presipn` 或其他變體）。
- 建議附上 Total RTP、Window Size、Denom 等欄位的實際數值。
- 禁止內容同 1.3。

## 4. 數學模型送驗版（送驗包 `PARsheet/` 資料夾）

送驗包 `文件/{GameID}/PARsheet/` 內的 `.xlsx` 是**數學模型送驗版**：以 `Source/*.xlsx` 為基礎，移除 1.3 禁止出現的項目（Extra Bet／Buy Feature／Super Feature 與 >$2 門檻相關的工作表、欄位與敘述）後另存。數學模型本體的規範見[數學模型規範.md](數學模型規範.md)；送驗交付面另要求：

### 4.1 Overview 必要欄位

以下欄位必須存在且有值：

| 欄位 | 對應欄位標題 |
|---|---|
| BG RTP | `Base Game Pay Back` |
| FG RTP | `Free Game Pay Back` |
| Bonus RTP | `Bonus Pay Back` |
| Progressive／Link RTP | `Link Pay Back` |
| 免費遊戲週期 | `Free Game Hits` |
| Scatter 出現機率 | `Scatter symbol appear rate` |

> 以程式讀取檢查時，含公式的儲存格可能讀為空值；此時以人工開檔確認，不得直接判為缺漏。

### 4.2 跨文件賠率一致性

- Game Description 的符號佔位符與數學模型送驗版的符號代號**按順序對應**（`[M1]`↔`W1`、`[M2]`↔`W2`，以此類推）。
- 逐符號比對 3 連、4 連、5 連的賠率值，任何不一致即為錯誤。
- 同一遊戲的多份數學模型送驗版（不同 RTP 版本）之間，非 RTP 差異欄位（如 Link Pay Back 設計）必須一致或有明確理由。

## 5. 送驗前檢查清單

- [ ] `文件/` 外層已有：`{GameID}_Help.xlsx`、`{GameID}_Game Description.docx`（完整版）、`{GameID}_Math Document Description.md`＋`_EN.md`、`PARsheet/`（精簡規格文件）。
- [ ] `文件/{GameID}/` 送驗包已依 1.1 的格式建立：`{GameID}_Game Description.docx`（送驗版）、`PARsheet/*.xlsx`（數學模型送驗版）、`Simulator/`（`Simulator.py`＋各 RTP `config_*.js`）均存在，檔名前綴與 Game ID 一致。
- [ ] Game Description 完整版與送驗版除刪減處外完全一致；完整版內容與 Help 完全相同。
- [ ] 數學模型送驗版已自 `Source/*.xlsx` 移除 1.3 禁止項目，其餘數學內容與正式數學模型一致。
- [ ] Game ID、中英文名稱與 `iGaming 遊戲代號一覽` 一致。
- [ ] 三份文件皆無 1.3 的禁止內容。
- [ ] Game Description 的格式來自已核准模板，內容與 Help 英文欄逐字一致、章節順序一致、無殘留 placeholder。
- [ ] Math Document Description 已含四大章與基本資訊表；`Phit` 公式使用 `Pscatter`，無變數錯字。
- [ ] 數學模型送驗版的 Overview 必要欄位齊全；檔名含 ParSheet ID。
- [ ] Game Description ↔ 數學模型送驗版賠率逐符號一致；多份 RTP 版本間一致。
- [ ] 文件內部、跨文件、中英對照的用詞一致。
- [ ] 送驗文件版本與數學模型、Config、Simulator 的正式版本相同。
